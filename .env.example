# Database
# PostgreSQL connection string
# DATABASE_URL is the main connection used by the application in all environments.
# DIRECT_URL is required for Prisma migrations and schema operations.
# 
# For Neon PostgreSQL (recommended for Vercel):
#   - DATABASE_URL should use the pooled connection (with -pooler in hostname)
#   - DIRECT_URL should use the direct connection (without -pooler)
# 
# For other providers (local, Supabase), use the same connection string for both.
# 
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# 
# Examples:
# - Local: postgresql://postgres:password@localhost:5432/supplyvault
# - Neon (pooled): postgresql://user:password@ep-xxxxx-pooler.region.aws.neon.tech/database?sslmode=require
# - Supabase: postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# DIRECT_URL - Direct connection required for migrations and schema operations
# For Neon: Same as DATABASE_URL but remove "-pooler" from hostname
# For other providers: Use the same connection string as DATABASE_URL
# 
# Example (Neon direct): postgresql://user:password@ep-xxxxx.region.aws.neon.tech/database?sslmode=require
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Clerk Authentication
# Get these from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_publishable_key_here
CLERK_SECRET_KEY=your_secret_key_here

# Clerk URL Configuration
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard/onboarding

# AWS S3 (for document uploads)
# Optional - only needed if using S3 for document storage
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your_bucket_name_here
NEXT_PUBLIC_AWS_S3_BUCKET_URL=https://your-bucket.s3.amazonaws.com

# Email Service (for alerts)
# Optional - only needed for email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
EMAIL_FROM=noreply@yourdomain.com

# Anthropic AI (for AI-powered certificate parsing - Phase 5)
# Get your API key from https://console.anthropic.com
# Optional - only needed if using AI certificate parsing
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx
